{
  "build": {
    "env": {
      "POSTMARK_SMTP_HOST": "smtp.postmarkapp.com",
      "POSTMARK_SMTP_PORT": "587",
      "POSTMARK_SMTP_USERNAME": "@smtp-username",
      "POSTMARK_SMTP_PASSWORD": "@smtp-password"
    },
    "functions": {
      "api/**/*.js"
    }
  }
}
